import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
//   eslint: {
//     // 🚨 TEMP ONLY: let the site deploy while we fix lint errors
//     ignoreDuringBuilds: true,
// };
};

export default nextConfig;
