// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  trailingSlash: false,
  // keep your existing settings here; no pageExtensions for mdx needed
};

export default nextConfig;
